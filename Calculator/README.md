Create project and initial commit — Cоздал пустой проект.

Add CalculatorViewModel skeleton and operations enum — создал файл логики, набросал структуру, но пока без методов.

Implement number typing logic in ViewModel — подготовил и проверил логику ввода цифр.

Implement math operations logic — добавил сложение, вычитание, умножение и деление.

Add divide by zero protection — обработал крайний случай с делением на ноль.

Create basic layout in ContentView — набросал сетку кнопок и экран, подключил их к логике.

Apply colors to UI — настроил цвета.
